#include <stdio.h>

void main()
{
    int a, b, max;
    printf("\n input two numbers:");
    scanf("%d%d", &a, &b);
    if (a > b)
    {
        printf("max = %d", a);
    }else
    {
        printf("max = %d", b);
    }
    

}